# Minimal PostgreSQL configuration for development
listen_addresses = '*'
port = 5432

# # Connection settings
# max_connections = 100
# superuser_reserved_connections = 3

# # Memory settings (minimal for development)
# shared_buffers = 32MB
# effective_cache_size = 128MB
# work_mem = 4MB
# maintenance_work_mem = 16MB

# # Logging settings (helpful for debugging)
# log_statement = 'all'
# log_min_duration_statement = 0
# log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h '

# # WAL settings (minimal for development)
# wal_level = minimal
# checkpoint_completion_target = 0.9
# wal_buffers = 16MB
# min_wal_size = 80MB
# max_wal_size = 320MB

# # Performance settings (conservative for development)
# effective_io_concurrency = 1
# random_page_cost = 1.1
# seq_page_cost = 1.0

# # Security settings
# password_encryption = trust